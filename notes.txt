

ttl--- to get time live
set(key,value)--- store single variable
hset--- store object
hgetall---like getting entire object


** Email ques with redis list
lpush - push value from left side
rpush - push value from right side
lpop - pop value from left side
rpop - pop value from right side


** BullMQ ***
Queue
producers- tasks sends at speed to workers
to  avoid workload we use Ques
Worker- handle the tasks

1.first create queue using BullMQ  and  create connection 
2.then create workers using worker from BullMQ and connection 
3.create state completed and failed and so on using worker eg. worker.on("complete",()=>{})
4.then create Apis of Add and Remove queues using ques which is created in queue.js file


*** pub/sub **
Its a  notification system by default comes with system
to send notification or anything to publisher to subscriber
to create this first create subscriber using redis url
create subscriber with channel we can create multiple channels

publisher publish msg to channel using channel name with msg
and subscriber actively listen to channels
